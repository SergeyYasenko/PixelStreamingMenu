# Тестирование pointer-events и взаимодействия

## ✅ Чек-лист для проверки на планшете

### 1. Тест жестов к video (UE приложению):

**Где тестировать:** Пустая область экрана (где НЕТ UI элементов)

- [ ] **Свайп вверх/вниз** → Камера двигается ✅
- [ ] **Свайп влево/вправо** → Камера поворачивается ✅
- [ ] **Пинч-зум** (если включен) → Работает ✅
- [ ] **Долгое нажатие** → Событие доходит до UE ✅
- [ ] **Двойной тап** → Работает ✅

**Ожидаемый результат:** Все жесты работают как в обычном Pixel Streaming!

### 2. Тест UI элементов:

**Где тестировать:** На самих UI компонентах

#### house-body (меню слева вверху):
- [ ] **Тап на корпусе** → Выбирается корпус ✅
- [ ] **Тап на этаже** → Выбирается этаж ✅
- [ ] **Скролл списка** → Список скроллится ✅

#### DisplayPositioning (кнопки справа):
- [ ] **Тап на кнопке "Машина"** → Срабатывает ✅
- [ ] **Тап на кнопке "Дрон"** → Срабатывает ✅
- [ ] **Раскрытие меню** → Работает ✅

#### BottomMenu (нижнее меню):
- [ ] **Тап на иконке камеры** → Открывается меню ✅
- [ ] **Тап на иконке погоды** → Открывается меню ✅
- [ ] **Выбор опции** → Работает ✅

#### ApartmentCard (карточка квартиры, если открыта):
- [ ] **Тап на кнопке закрытия** → Карточка закрывается ✅
- [ ] **Скролл информации** → Скролл работает ✅

**Ожидаемый результат:** Все UI элементы кликабельны и работают нормально!

### 3. Тест границ (критичный!):

**Где тестировать:** Рядом с краями UI элементов

- [ ] **Тап РЯДОМ с меню** → Событие идёт к video ✅
- [ ] **Тап МЕЖДУ кнопками** → Событие идёт к video ✅
- [ ] **Свайп через меню** → Начинается на меню (не работает), но в пустой области работает ✅

**Ожидаемый результат:** События проходят к video везде кроме самих UI элементов!

## 🐛 Возможные проблемы и решения

### Проблема 1: Жесты НЕ проходят к video нигде

**Причина:** `connected-display` имеет `pointer-events: auto` вместо `none`

**Проверка:**
```css
/* Должно быть так: */
.connected-display {
   pointer-events: none; /* ✅ */
}
```

**Решение:** Измените на `pointer-events: none`

### Проблема 2: UI элементы не кликабельны

**Причина:** Дочерние элементы не имеют `pointer-events: auto`

**Проверка:**
```css
/* Проверьте что у всех интерактивных элементов: */
.house-body,
.bottom-menu-wrapper,
.display-positioning {
   pointer-events: auto; /* ✅ */
}
```

**Решение:** Добавьте `pointer-events: auto` к интерактивным элементам

### Проблема 3: События проходят ЧЕРЕЗ UI элементы

**Причина:** `z-index` не настроен правильно

**Проверка:**
```css
.video-container { z-index: 0; }     /* ✅ Снизу */
.connected-display { z-index: 10; }  /* ✅ Сверху */
```

**Решение:** Убедитесь что UI выше video по z-index

### Проблема 4: Меню визуально перекрывает video

**Причина:** UI элементы не имеют полупрозрачный фон или backdrop-filter

**Проверка:** Посмотрите на меню - видно ли video под ними?

**Решение:**
```css
/* UI должны быть полупрозрачными: */
.house-body {
   background-color: rgba(34, 34, 34, 0.5); /* ✅ */
   backdrop-filter: blur(5px); /* опционально */
}
```

## 🔬 Debug через DevTools

### Проверка pointer-events в Chrome DevTools:

1. Откройте **DevTools** (F12)
2. **Elements** → Выберите элемент
3. **Computed** → Найдите `pointer-events`
4. Проверьте значение:
   - `none` → События проходят НАСКВОЗЬ ✅
   - `auto` → Элемент ПЕРЕХВАТЫВАЕТ события ✅

### Визуализация слоёв:

1. **DevTools** → **More tools** → **Layers** (если доступно)
2. Проверьте порядок слоёв:
   - Video снизу
   - UI сверху

### Отладка событий:

```javascript
// В консоли браузера:
document.addEventListener('touchstart', (e) => {
   console.log('Touch на:', e.target);
}, true);

// Тапните на разные области:
// - Пустая область → Должен показать video или canvas
// - UI элемент → Должен показать div меню
```

## 📊 Тестовые сценарии

### Сценарий A: Навигация камерой ПОД UI
1. Положите палец на пустую область (где НЕТ меню)
2. Двигайте палец вверх-вниз
3. **Ожидается:** Камера в UE движется плавно

### Сценарий B: Выбор корпуса
1. Тапните на меню корпусов (слева вверху)
2. Выберите другой корпус
3. **Ожидается:** Корпус выбирается, камера НЕ двигается

### Сценарий C: Смешанное взаимодействие
1. Начните свайп на меню
2. Продолжите свайп на пустую область
3. **Ожидается:** 
   - На меню: Скролл меню ИЛИ выбор (зависит от реализации)
   - На пустой области: Камера НЕ двигается (жест начался на UI)

### Сценарий D: Нижнее меню
1. Тапните на иконку в нижнем меню
2. Откроется подменю
3. **Ожидается:** Подменю открывается, камера НЕ реагирует

### Сценарий E: Карточка квартиры
1. Откройте карточку квартиры
2. Попробуйте свайп на карточке
3. Попробуйте свайп РЯДОМ с карточкой
4. **Ожидается:**
   - На карточке: Скролл внутри карточки
   - Рядом с карточкой: Камера двигается

## ✅ Успешный результат

Тест считается **пройденным**, если:

1. ✅ Жесты работают в пустых областях (камера двигается)
2. ✅ UI элементы кликабельны и реагируют на тач
3. ✅ События НЕ проходят ЧЕРЕЗ UI элементы к video
4. ✅ Границы между UI и пустыми областями чёткие
5. ✅ Video не перекрыт непрозрачным overlay
6. ✅ z-index правильный (UI поверх video визуально)

## 🎯 Почему это работает

### CSS Cascading для pointer-events:

```html
<!-- Родитель: события проходят -->
<div class="connected-display" style="pointer-events: none">
   
   <!-- Ребёнок: события НЕ проходят, перехватываются -->
   <div class="house-body" style="pointer-events: auto">
      <button>Клик меня!</button>  ✅ Работает!
   </div>
   
   <!-- Пустая область: события проходят к video -->
   <div class="empty-area">  ✅ События идут к video!
   </div>
   
</div>
```

### Стекинг (z-index):

```
z-index: 15  →  bottom-menu-wrapper  (самый верхний)
z-index: 15  →  DisplayPositioning
z-index: 10  →  connected-display
z-index: 10  →  ApartmentCard
z-index: 5   →  house-body
z-index: 0   →  video-container  (самый нижний)
```

**Визуально:** UI сверху, video снизу  
**События:** UI перехватывает где есть, video получает где нет UI

## 🆘 Если ничего не помогло

1. Проверьте консоль браузера на ошибки
2. Убедитесь что браузер планшета поддерживает CSS pointer-events
3. Попробуйте в другом браузере (Chrome vs Safari vs Samsung Internet)
4. Сделайте скриншот DevTools → Elements со стилями
5. Проверьте что нет глобальных стилей перекрывающих pointer-events

---

**См. также:**
- `TABLET_VIDEO_RENDERING_FIX.md` - Исправление рендеринга
- `../src/components/ConnectedDisplay.vue` - Реализация

**Все документы находятся в папке `docs/`**

