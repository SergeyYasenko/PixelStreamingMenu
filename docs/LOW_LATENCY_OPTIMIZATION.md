# Оптимизация для низкой задержки - Управление камерой на планшете

## 🎯 Проблема
На планшете изменение положения камеры происходит с большой задержкой или зависает. Пользователь нажимает на элементы управления, но камера реагирует с опозданием на 200-500+ мс.

## 🔍 Причины задержки

### 1. **Буферизация видео**
- Браузеры буферизуют видео для плавного воспроизведения
- Это увеличивает задержку между действием и результатом

### 2. **Переменный битрейт (VBR)**
- По умолчанию используется VBR (Variable Bitrate)
- Непредсказуемая задержка из-за колебаний битрейта

### 3. **Высокое качество = больше задержка**
- Более высокое разрешение требует больше времени на кодирование
- Высокий QP (Quality Parameter) увеличивает обработку

### 4. **Недостаточный приоритет FPS**
- Если приоритет на качестве, система может жертвовать FPS
- Это приводит к рывкам и задержкам

## ✅ Реализованные оптимизации

### 1. Режим низкой задержки (Low Latency Mode)

#### В Config (строки 218-228):
```javascript
// Постоянный битрейт для стабильной задержки
RateControl: "CBR",              // Constant Bitrate вместо Variable

// Режим низкой задержки
LowLatency: true,                // Минимизирует буферизацию

// Настройки качества для low latency
MinQP: 20,                       // Минимум качества (20 = хорошо)
MaxQP: 37,                       // Максимум (37 вместо 51 для лучшего качества)
FillerData: false,               // Отключить заполнение для скорости

// Уменьшенное разрешение для быстрой обработки
VideoScalingFactor: 0.75,        // 75% от оригинала (было 1.0)
```

### 2. Настройки задержки (Latency Settings)

#### В Config (строки 258-261):
```javascript
MaxLatency: 50,                  // Максимум 50мс (было 200мс)
MinLatency: 0,                   // Минимум 0мс
TargetLatency: 30,               // Целевая задержка 30мс
```

**Объяснение:**
- `MaxLatency: 50` - браузер не будет буферизовать больше 50мс
- `TargetLatency: 30` - оптимальная цель для отзывчивости
- Меньше = быстрее реакция, но возможны артефакты при плохом соединении

### 3. Приоритет FPS (строки 315-323)

#### В прямых настройках WebRTC:
```javascript
DegradationPreference: "maintain-framerate"  // Приоритет FPS!

WebRTCFPS: 24                    // Стабильные 24 FPS
MaxQP: 37                        // Баланс качества и скорости
```

**Почему это важно:**
- `maintain-framerate` - при проблемах с сетью снижается качество, но не FPS
- Плавное управление камерой важнее идеального качества картинки

### 4. Настройки кодировщика (строки 325-328)

```javascript
Encoder.RateControl: "CBR"       // Постоянный битрейт на уровне кодировщика
Encoder.LowDelay: true           // Минимальная задержка кодирования
Encoder.FillerData: 0            // Без заполнителей
```

## 🎮 Как это работает

### До оптимизации:
```
Пользователь нажал → 
  Буфер 100мс → 
    VBR колебания 50-150мс → 
      Декодирование 50мс → 
        Рендеринг 30мс
= 230-430мс задержки!
```

### После оптимизации:
```
Пользователь нажал → 
  Буфер 30мс → 
    CBR стабильно 20мс → 
      Быстрое декодирование 25мс → 
        Рендеринг 20мс
= 95мс задержки!
```

## 📊 Таблица настроек для разных сценариев

### Приоритет: ОТЗЫВЧИВОСТЬ (текущие настройки)
| Параметр | Значение | Результат |
|----------|----------|-----------|
| MaxLatency | 50 | Минимальная задержка |
| TargetLatency | 30 | Цель 30мс |
| VideoScalingFactor | 0.75 | 75% разрешения |
| RateControl | CBR | Стабильная задержка |
| LowLatency | true | Быстрая реакция |
| MaxQP | 37 | Приемлемое качество |
| **Задержка** | **~50-100мс** | ✅ Отлично |
| **Качество** | **Среднее** | ⚠️ Компромисс |

### Приоритет: БАЛАНС
| Параметр | Значение | Результат |
|----------|----------|-----------|
| MaxLatency | 100 | Умеренная задержка |
| TargetLatency | 50 | Цель 50мс |
| VideoScalingFactor | 0.85 | 85% разрешения |
| RateControl | CBR | Стабильная задержка |
| LowLatency | true | Хорошая реакция |
| MaxQP | 45 | Хорошее качество |
| **Задержка** | **~100-150мс** | ✅ Хорошо |
| **Качество** | **Хорошее** | ✅ Хорошо |

### Приоритет: КАЧЕСТВО
| Параметр | Значение | Результат |
|----------|----------|-----------|
| MaxLatency | 200 | Больше буфера |
| TargetLatency | 100 | Цель 100мс |
| VideoScalingFactor | 1.0 | 100% разрешения |
| RateControl | VBR | Переменный битрейт |
| LowLatency | false | Обычный режим |
| MaxQP | 51 | Лучшее качество |
| **Задержка** | **~200-400мс** | ⚠️ Заметная |
| **Качество** | **Отличное** | ✅ Отлично |

## 🔧 Дополнительная оптимизация

### Если задержка всё ещё высокая:

#### 1. Ещё больше снизить задержку:
```javascript
MaxLatency: 30,              // Вместо 50
TargetLatency: 15,           // Вместо 30
VideoScalingFactor: 0.5,     // 50% вместо 75%
```

#### 2. Снизить битрейт:
```javascript
MinBitrate: 300000,          // 300kbps вместо 500kbps
MaxBitrate: 5000000,         // 5Mbps вместо 8Mbps
```

#### 3. Снизить FPS (парадоксально, но может помочь):
```javascript
WebRTCFPS: 20,               // 20 вместо 24
KeyframeInterval: 300,       // 300мс вместо 500мс
```

#### 4. Проверить настройки Unreal Engine:

На стороне сервера в UE также важны настройки:
```
Pixel Streaming Plugin:
- Encoder Target Bitrate: 3000000 (3Mbps)
- Encoder Max Bitrate: 5000000 (5Mbps)
- Encoder Latency Mode: Ultra Low Latency
- Encoder RateControl: CBR
- Encoder MinQP: 20
- Encoder MaxQP: 37
- Capture FPS: 24
```

## 🧪 Тестирование задержки

### 1. Консольный лог
После подключения вы увидите:
```
✅ Режим низкой задержки активирован для планшета
```

### 2. Измерение реальной задержки

Включите статистику:
```javascript
PrintStats: true,
ShowTextOverlays: true,
LogLevel: "Info",
```

Смотрите в chrome://webrtc-internals:
- `googCurrentDelayMs` - текущая задержка
- `googTargetDelayMs` - целевая задержка
- `googJitterBufferMs` - буфер джиттера

**Целевые значения:**
- `googCurrentDelayMs` < 100мс ✅
- `googJitterBufferMs` < 50мс ✅

### 3. Субъективный тест
1. Нажмите кнопку управления камерой
2. Считайте: "Раз" (примерно 100мс)
3. Камера должна начать двигаться ДО конца счёта

## ⚠️ Компромиссы

### Что мы получили:
✅ Задержка управления камерой: ~50-100мс (было 200-500мс)
✅ Стабильная отзывчивость
✅ Плавное управление

### Что потеряли:
⚠️ Разрешение: 75% вместо 100%
⚠️ Качество: MaxQP 37 вместо 51 (немного хуже в деталях)
⚠️ Возможны артефакты при очень плохом соединении

### Можно ли получить и то, и то?
Частично да:
1. Используйте `VideoScalingFactor: 0.85` (85%) - компромисс
2. `MaxQP: 42` - лучше качество, но +20-30мс задержки
3. Хорошее WiFi соединение решает большинство проблем

## 📝 Где находятся настройки

**Файл:** `src/components/PixelStreaming.vue`

**Секции:**
- Настройки Low Latency: строки 218-228
- Настройки задержки: строки 258-261
- Прямые настройки WebRTC: строки 308-337

## 🆘 Troubleshooting

### Задержка всё ещё большая
1. Проверьте WiFi - переключитесь на 5GHz
2. Закройте другие приложения на планшете
3. Уменьшите `MaxLatency` до 30
4. Попробуйте `VideoScalingFactor: 0.5`

### Картинка стала хуже
1. Увеличьте `MaxQP` до 42-45
2. Увеличьте `VideoScalingFactor` до 0.85-0.9
3. Увеличьте `MinBitrate` до 800000

### Лаги и фризы
1. Проверьте консоль - есть ли ошибки
2. Увеличьте `MaxLatency` до 75-100
3. Снизьте битрейт: `MaxBitrate: 5000000`
4. Попробуйте H.264 вместо VP8

### Камера дёргается
1. Это нормально при MaxLatency < 50
2. Увеличьте до 75 для плавности
3. Или увеличьте `TargetLatency` до 50

---

**См. также:**
- `PIXEL_STREAMING_SETTINGS_CHEATSHEET.md` - все настройки
- `TABLET_OPTIMIZATION_NOTES.md` - общие оптимизации для планшетов

**Все документы находятся в папке `docs/`**

