# Исправление проблемы рендеринга видео на планшетах

## 🔍 Проблема

### Симптомы:
- ✅ На **мобильных телефонах** всё работает отлично
- ✅ **Pixel Streaming сам по себе** (без Vue) работает без проблем
- ❌ Через **Vue3 интерфейс на планшете**: картинка "замораживается" на 5+ секунд
- ❌ При этом на **сервере Pixel Streaming** камера движется плавно

### Важно:
- Проблема **НЕ в сети** (задержки нет)
- Проблема **НЕ в Pixel Streaming** (сервер работает нормально)
- Проблема **НЕ в планшете** (сам PS работает отлично)
- Проблема **в рендеринге video элемента** через браузер на планшете

## 🎯 Корневая причина

### GPU Compositor Layering Conflict

Планшеты обрабатывают композитинг GPU слоёв иначе чем телефоны и десктопы:

1. **Video элемент с CSS transforms**
   ```css
   transform: scaleX(-1);
   will-change: transform;
   transform-style: preserve-3d;
   -webkit-transform: translateZ(0) scaleX(-1);
   ```
   ↓
   Браузер создаёт **отдельный GPU композитный слой**

2. **UI Overlay (ConnectedDisplay) сверху**
   ```css
   position: absolute;
   z-index: 5;
   pointer-events: auto;
   ```
   ↓
   Браузер создаёт **ещё один GPU композитный слой**

3. **Конфликт на планшетах:**
   - Слишком много GPU оптимизаций (`will-change`, `preserve-3d`, `translateZ(0)`)
   - Video не имеет явного `z-index` → браузер не знает порядок слоёв
   - Overlay блокирует обновление video слоя
   - Браузер планшета **кэширует video кадр** и не обновляет его

### Почему работает на телефонах?
- Меньше разрешение → меньше нагрузка на GPU
- Другая реализация браузера (часто Chrome Mobile vs Samsung/Huawei браузеры на планшетах)
- Меньше слоёв композитинга

### Почему не работает на планшетах?
- Больше разрешение → сложнее композитинг
- Производители планшетов (Samsung, Huawei) используют **кастомные браузеры** с агрессивными оптимизациями
- Браузер решает, что video не меняется (т.к. overlay сверху) и **прекращает его обновлять**

## ✅ Решение

### 1. Удалены избыточные GPU оптимизации

**Было (ПЛОХО для планшетов):**
```css
.video-container :deep(video) {
   transform: scaleX(-1);
   will-change: transform;              /* ❌ Избыточно */
   backface-visibility: hidden;         /* ❌ Избыточно */
   transform-style: preserve-3d;        /* ❌ Конфликтует */
   -webkit-transform: translateZ(0);    /* ❌ Дублирование */
   -webkit-font-smoothing: antialiased; /* ❌ Для текста, не видео */
}
```

**Стало (ХОРОШО для планшетов):**
```css
.video-container :deep(video) {
   transform: scaleX(-1);               /* ✅ Только необходимое */
   width: 100%;                         /* ✅ Явные размеры */
   height: 100%;
   object-fit: contain;                 /* ✅ Правильное масштабирование */
   display: block;                      /* ✅ Блочный элемент */
   position: relative;                  /* ✅ Явное позиционирование */
   z-index: 1;                          /* ✅ Явный слой */
}
```

### 2. Установлена явная иерархия z-index

**Было:**
```css
.video-container {
   position: relative;  /* ❌ Относительно чего? */
   /* z-index отсутствует ❌ */
}

.connected-display {
   position: absolute;
   /* z-index отсутствует ❌ */
}
```

**Стало:**
```css
.video-container {
   position: absolute;  /* ✅ Явное позиционирование */
   z-index: 0;         /* ✅ Video СНИЗУ */
   isolation: isolate;  /* ✅ Изоляция слоя */
}

.connected-display {
   position: absolute;
   z-index: 10;        /* ✅ UI СВЕРХУ */
}
```

### 3. Добавлена изоляция слоёв

```css
.video-container {
   isolation: isolate;  /* КРИТИЧНО для планшетов! */
}
```

**Что делает `isolation: isolate`:**
- Создаёт новый stacking context
- Изолирует video слой от overlay
- Браузер **гарантированно обновляет** video независимо от overlay
- Предотвращает блокировку рендеринга

## 📊 Технические детали

### Порядок рендеринга (До исправления):
```
GPU Compositor:
├─ Layer 1: video (transform: scaleX, will-change, preserve-3d, translateZ)
│  └─ Много GPU оптимизаций → сложный композитинг
├─ Layer 2: ConnectedDisplay (position: absolute)
│  └─ Блокирует Layer 1 на планшетах
└─ Layer 3: UI элементы (z-index: 5)
   └─ Ещё больше сложности

❌ Браузер планшета: "Слишком сложно, кэширую video кадр"
```

### Порядок рендеринга (После исправления):
```
GPU Compositor:
├─ Isolated Context: video-container (isolation: isolate, z-index: 0)
│  └─ Layer 1: video (простой transform, явные размеры, z-index: 1)
│     ✅ Простой композитинг, браузер обновляет каждый кадр
├─ Layer 2: ConnectedDisplay (z-index: 10, pointer-events: none)
│  └─ Не блокирует video
└─ Layer 3: UI элементы (z-index: 5, pointer-events: auto)
   ✅ Взаимодействие работает

✅ Браузер планшета: "Понятно, video слой изолирован, обновляю"
```

## 🧪 Тестирование

### Как проверить что исправление работает:

1. **Откройте на планшете**
2. **Подключитесь к Pixel Streaming**
3. **Двигайте камерой в UE**

**Ожидаемый результат:**
- ✅ Картинка обновляется **каждый кадр**
- ✅ Задержки нет (кроме сетевой ~20-50мс)
- ✅ Плавное движение как в самом Pixel Streaming
- ✅ UI элементы работают нормально

### Debug в DevTools:

1. Откройте **DevTools** → **Layers** (если доступно)
2. Проверьте что video и UI - **отдельные слои**
3. В **Performance** → запишите сессию
4. Проверьте что **video не пропускает кадры**

### Chrome DevTools Rendering:

```javascript
// В консоли
// Проверить что video обновляется
const video = document.querySelector('video');
setInterval(() => {
   console.log('Video currentTime:', video.currentTime);
}, 100);
// currentTime должен постоянно увеличиваться
```

## 🔧 Альтернативные решения (если проблема сохранится)

### Вариант A: Полное отключение transforms
```css
.video-container :deep(video) {
   transform: none;  /* Без зеркалирования */
   scale: -1 1;     /* Вместо transform (современный CSS) */
}
```

### Вариант B: Hardware acceleration принудительно
```css
.video-container :deep(video) {
   transform: scaleX(-1) translateZ(0.1px); /* Минимальный 3D */
}
```

### Вариант C: Изменить структуру DOM
```vue
<!-- Поместить UI в отдельный контейнер вне video-wrapper -->
<div class="pixel-streaming-container">
   <div class="video-layer">
      <div ref="videoContainer"></div>
   </div>
   <div class="ui-layer">
      <ConnectedDisplay />
   </div>
</div>
```

## 📝 Изменённые файлы

### `src/components/PixelStreaming.vue`
**Строки 433-458:** CSS для video-container и video элемента

**Изменения:**
- ✅ Добавлен `z-index: 0` на `.video-container`
- ✅ Добавлен `isolation: isolate`
- ✅ Удалены избыточные GPU оптимизации
- ✅ Добавлены явные размеры и `display: block`
- ✅ Добавлен `position: relative` и `z-index: 1` на video

### `src/components/ConnectedDisplay.vue`
**Строка 349:** Добавлен `z-index: 10` на `.connected-display`

**Изменения:**
- ✅ Явная иерархия слоёв (UI поверх video)

## 🎯 Взаимодействие: UI поверх video

### Вопрос: Не будет ли UI перекрывать video и блокировать жесты?

**Ответ: НЕТ!** Благодаря `pointer-events`:

```css
/* Родительский контейнер: события ПРОХОДЯТ НАСКВОЗЬ */
.connected-display {
   pointer-events: none;  ✅
   z-index: 10;          ✅
}

/* Дочерние UI элементы: ПЕРЕХВАТЫВАЮТ события */
.house-body,
.bottom-menu-wrapper,
.display-positioning {
   pointer-events: auto;  ✅
}
```

### Как это работает:

1. **В пустых областях** (где нет UI)
   - События **проходят через** `connected-display`
   - Достигают `video-container`
   - Жесты работают с UE приложением ✅

2. **На UI элементах** (меню, кнопки)
   - События **перехватываются** UI элементом
   - НЕ проходят к video
   - UI работает нормально ✅

3. **Визуально**
   - UI **поверх** video (`z-index: 10` > `z-index: 0`)
   - UI полупрозрачный → видно video под ним
   - Нет конфликта! ✅

**Детальное тестирование:** См. `POINTER_EVENTS_TESTING.md`

## ⚠️ Важно

### Что НЕ нужно делать:

❌ **Не добавляйте обратно GPU оптимизации**
```css
/* НЕ ДОБАВЛЯТЬ на планшетах! */
will-change: transform;
transform-style: preserve-3d;
-webkit-transform: translateZ(0);
backface-visibility: hidden;
```

❌ **Не меняйте z-index без понимания**
- Video: `z-index: 0`
- UI: `z-index: 10`
- Эта иерархия критична!

❌ **Не убирайте `isolation: isolate`**
- Это ключевое свойство для планшетов

### Что можно настроить:

✅ **object-fit** (если нужно другое масштабирование)
```css
object-fit: cover;  /* Заполнить весь контейнер */
object-fit: fill;   /* Растянуть */
```

✅ **Направление зеркала** (если нужно)
```css
transform: scaleX(-1);  /* Горизонтально */
transform: scaleY(-1);  /* Вертикально */
```

## 🎓 Почему это работает

### Принцип изоляции слоёв:

1. **`isolation: isolate`** создаёт новый stacking context
2. Браузер **гарантирует** что содержимое этого context'а рендерится независимо
3. Overlay (ConnectedDisplay) не может блокировать обновление video
4. GPU композитор обрабатывает слои раздельно

### Упрощение композитинга:

- **Меньше GPU директив** = проще для браузера
- **Явные размеры** = браузер не пересчитывает layout каждый кадр
- **Чёткая иерархия z-index** = браузер точно знает порядок отрисовки

## 🆘 Если проблема сохраняется

### 1. Проверьте версию браузера планшета
Некоторые старые браузеры не поддерживают `isolation: isolate`

### 2. Попробуйте отключить transform
```css
transform: none;
```

### 3. Проверьте GPU в DevTools
```
chrome://gpu
```
Убедитесь что GPU композитинг включён

### 4. Проверьте конкретную модель планшета
Создайте issue с указанием:
- Модель планшета
- Версия ОС
- Браузер и версия
- Скриншоты DevTools → Layers

---

**См. также:**
- `TABLET_OPTIMIZATION_NOTES.md` - Общие оптимизации
- `LOW_LATENCY_OPTIMIZATION.md` - Оптимизация задержки

**Все документы находятся в папке `docs/`**

