# Оптимизации для планшетов - Pixel Streaming

## Проблема
На планшетах картинка в Pixel Streaming не обновляется, хотя на сервере все работает корректно. На телефонах проблемы нет.

## Причины
1. **WebRTC оптимизация браузеров** - планшеты агрессивно оптимизируют WebRTC соединения
2. **Аппаратное ускорение** - проблемы с GPU рендерингом видео элемента
3. **Управление энергопотреблением** - планшеты могут снижать FPS для экономии батареи
4. **Ключевые кадры** - недостаточная частота I-frames приводит к "замороженной" картинке

## ⚠️ Важное примечание
**Ручная оптимизация с принудительным обновлением кадров УДАЛЕНА!**

Причина: Принудительное обновление видео элемента каждые 2 секунды вызывало проблемы с плавностью:
- Картинка обновлялась рывками, а не плавно
- Видимые переходы между кадрами
- Неестественное поведение видео

**Решение:** Полагаемся только на настройки Config и нативные возможности WebRTC.
Если проблемы с обновлением картинки сохраняются - настраивайте параметры Config, особенно `KeyframeInterval`.

## Реализованные решения

### 1. Настройки Config (строки 156-289) - ПОЛНЫЙ СПИСОК

Все настройки теперь документированы с дефолтными значениями и пояснениями.

#### Основные настройки подключения:
```javascript
ss: signallingUrl.value    // URL WebSocket сервера
AutoPlayVideo: true         // Автовоспроизведение (default: false)
AutoConnect: true           // Автоподключение (default: false)
ss_autoconnect: true        // Альтернативный флаг (default: false)
```

#### Настройки видео и аудио:
```javascript
StartVideoMuted: false      // Без звука при старте (default: false)
HoveringMouse: true         // Курсор при наведении (default: true)
FakeMouseWithTouches: true  // Эмуляция мыши тачем (default: true)
UseMic: false               // Использовать микрофон (default: false)
UseAudio: false             // Использовать аудио (default: false)
```

#### Настройки логирования:
```javascript
LogLevel: "Error"           // Verbose|Info|Warning|Error|None (default: "Info")
```

#### Настройки видео кодека:
```javascript
PreferH264: true            // Предпочитать H.264 (default: false)
ForceH264: true             // Принудительно H.264 (default: false)
PreferVP8: false            // Предпочитать VP8 (default: false)
PreferVP9: false            // Предпочитать VP9 (default: false)
```

#### Настройки сети:
```javascript
ForceTURN: false            // Принудительно TURN (default: false)
ForceMONO: false            // Моно аудио (default: false)
```

#### Настройки битрейта (критично для планшетов):
```javascript
MinBitrate: 500000          // 500 kbps (default: 100000)
MaxBitrate: 8000000         // 8 Mbps (default: 20000000)
LowBitrateTimeoutMs: 10000  // Таймаут 10 сек (default: 10000)
WebRTCMinBitrate: 500000    // WebRTC минимум (default: не задан)
WebRTCMaxBitrate: 8000000   // WebRTC максимум (default: не задан)
WebRTCFPS: 30               // Максимальный FPS (default: 60)
```

#### Настройки качества видео:
```javascript
VideoScalingFactor: 1.0     // Масштаб 100% (default: 1.0)
KeyframeInterval: 1000      // I-frames каждую секунду (default: 2000)
                            // КРИТИЧНО для планшетов!
```

#### Настройки управления:
```javascript
SuppressBrowserKeys: true   // Блокировка горячих клавиш (default: true)
IsQualityController: false  // Управление качеством (default: false)
```

#### Настройки разрешения:
```javascript
MatchViewportResolution: true // Подстройка под viewport (default: false)
                              // КРИТИЧНО для планшетов!
```

#### Настройки стримера:
```javascript
StreamerAutoJoin: true      // Автоподключение к стримеру (default: false)
```

#### Настройки XR:
```javascript
XRControllerInput: false    // Поддержка VR/AR контроллеров (default: false)
```

#### Экспериментальные настройки:
```javascript
TimeoutIfIdle: false        // Таймаут при бездействии (default: false)
MaxReconnectAttempts: 3     // Попытки переподключения (default: 3)
MaxLatency: 200             // Макс задержка в мс (default: не задан)
MinLatency: 0               // Мин задержка в мс (default: 0)
```

#### Настройки для touch устройств:
```javascript
TouchScreenInput: true      // Поддержка тача (default: true)
GamepadInput: false         // Поддержка геймпада (default: true)
```

#### Дополнительные настройки:
```javascript
AFKTimeout: 0               // Таймаут AFK (0 = выкл) (default: 0)
WaitForStreamer: true       // Ждать стример (default: true)
PrintStats: false           // Статистика в консоль (default: false)
ShowTextOverlays: false     // Текстовые оверлеи (default: false)
```

### 2. CSS оптимизации (строки 439-456)
```css
will-change: transform;
backface-visibility: hidden;
transform-style: preserve-3d;
-webkit-transform: translateZ(0) scaleX(-1);
```
Эти свойства принуждают браузер использовать аппаратное ускорение GPU для плавного рендеринга.

### 3. Прямая настройка WebRTC (строки 295-310)
```javascript
DegradationPreference: "maintain-framerate"  // Приоритет FPS над качеством
MaxFPS: 30                                   // Ограничение FPS
MinQP: 20                                    // Минимальное качество
MaxQP: 51                                    // Максимальное качество
```

### 4. Мониторинг статистики (строки 371-381)
Отслеживание получения и декодирования кадров для диагностики.
Предупреждения в консоль если планшет не получает кадры.

## Как тестировать

### Шаг 1: Проверка консоли
Откройте DevTools на планшете и проверьте:
- ⚠️ Предупреждения "Планшет не получает кадры" (если есть проблемы с WebRTC)

### Шаг 2: Проверка WebRTC статистики
В консоли проверьте:
```javascript
// Должны увеличиваться
framesReceived
framesDecoded
bytesReceived
```

### Шаг 3: Тестирование сценариев
1. Поворот планшета (портрет ↔ пейзаж)
2. Переключение вкладок
3. Переход в фоновый режим и возврат
4. Длительная работа (30+ минут)

## Дополнительные настройки (если проблема сохраняется)

### Вариант A: Увеличить частоту ключевых кадров
```javascript
KeyframeInterval: 500 // Каждые 500ms вместо 1000ms
```

### Вариант B: Снизить битрейт
```javascript
MaxBitrate: 5000000 // 5 Mbps вместо 8 Mbps
```

### Вариант C: Снизить FPS
```javascript
WebRTCFPS: 24 // Вместо 30
```

### Вариант D: Включить VP8 вместо H.264
```javascript
PreferH264: false
ForceH264: false
PreferVP8: true
```

### Вариант E: Настройки Unreal Engine
В проекте UE проверьте:
```
Pixel Streaming Plugin Settings:
- Encoder Target Bitrate: 5000000
- Encoder Max Bitrate: 8000000
- WebRTC FPS: 30
- Keyframe Interval: 1000ms
```

## Известные проблемы планшетов

### iPad
- Safari использует собственную реализацию WebRTC
- Может требовать VP8 вместо H.264
- Агрессивная оптимизация энергопотребления

### Android планшеты
- Chrome может throttle WebRTC в фоне
- GPU может переключаться в энергосберегающий режим
- Различные производители (Samsung, Huawei) имеют свои оптимизации

## Диагностика

Если проблема сохраняется, соберите информацию:
1. Модель планшета и версия ОС
2. Браузер и версия
3. Логи консоли (особенно WebRTC stats)
4. chrome://webrtc-internals (для Chrome/Edge)
5. Разрешение экрана планшета

## Быстрый справочник по настройкам

### 🎯 Самые важные для планшетов:
| Настройка | Значение | Зачем |
|-----------|----------|-------|
| `KeyframeInterval` | 1000 | Частые I-frames = стабильная картинка |
| `MatchViewportResolution` | true | Подстройка под разрешение планшета |
| `WebRTCFPS` | 30 | Ограничение FPS для стабильности |
| `MinBitrate` / `MaxBitrate` | 500k / 8M | Оптимальный диапазон для планшетов |
| `VideoScalingFactor` | 1.0 | Без масштабирования |

### 🔧 Настройки для разных устройств:

#### 📱 Телефоны:
```javascript
MinBitrate: 300000
MaxBitrate: 5000000
VideoScalingFactor: 0.75
WebRTCFPS: 30
KeyframeInterval: 1500
```

#### 📋 Планшеты (текущие):
```javascript
MinBitrate: 500000
MaxBitrate: 8000000
VideoScalingFactor: 1.0
WebRTCFPS: 30
KeyframeInterval: 1000
```

#### 💻 Десктопы:
```javascript
MinBitrate: 1000000
MaxBitrate: 20000000
VideoScalingFactor: 1.0
WebRTCFPS: 60
KeyframeInterval: 2000
```

### 🚨 Если картинка "замораживается":

1. **Уменьшите KeyframeInterval:**
   ```javascript
   KeyframeInterval: 500  // Вместо 1000
   ```

2. **Попробуйте VP8 вместо H.264:**
   ```javascript
   PreferH264: false
   ForceH264: false
   PreferVP8: true
   ```

3. **Снизьте FPS:**
   ```javascript
   WebRTCFPS: 24  // Вместо 30
   ```

4. **Включите отладку:**
   ```javascript
   LogLevel: "Info"
   PrintStats: true
   ShowTextOverlays: true
   ```

### 📊 Полный список всех настроек:

Все 30+ настроек с дефолтными значениями и пояснениями находятся в файле:
`src/components/PixelStreaming.vue` (строки 203-336)

Каждая настройка документирована в формате:
```javascript
SettingName: value, // Описание (default: defaultValue)
```

## Контакты для дебага
Файл: `src/components/PixelStreaming.vue`
Основные функции:
- `connect()` - строка 146
- `setupMouseInterception()` - строка 75

Все настройки Config: строки 156-289

